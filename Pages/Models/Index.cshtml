@page
@model TestCore3.Pages.Models.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Model) {
        <tr>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Naam">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Naam">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Naam">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
